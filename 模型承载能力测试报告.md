# 大学物理智能助教模型承载能力测试报告

> 测试日期：2026-08-09 至 2026-08-10  
> 测试模型：`qwen/qwen3-vl-30b`、`qwen/qwen3.6-27b`、`deepseek/deepseek-v4-flash`  
> API：`http://192.168.222.147:1234/v1`（OpenAI 兼容接口）  
> 用途：模型选型、并发限制、RAG 参数和生产部署参考  
> 部署状态：2026-08-10 已将项目对话与图片识别切换为 `tianwen` 上的 `qwen/qwen3.6-27b`

## 1. 结论摘要

| 项目 | Qwen3-VL-30B | Qwen3.6-27B | DeepSeek V4 Flash |
|---|---:|---:|---:|
| 主要定位 | 多模态问答、图片识题 | 长上下文、多模态候选 | 纯文本推理与讲解 |
| 运行位置 | 本机 LM Studio | LM Link 设备 `tianwen` | 本机 LM Studio / 原生服务 |
| 模型文件规模 | 约 19.64 GB（含视觉投影） | 29.53 GB | 156.38 GB |
| 量化 | Q4_K_M | Q8_0 | MXFP4 |
| 服务配置上下文 | 8192 token | 262144 token | 8192 token |
| 本次验证上下文 | 7913 token | 16854 token | 3216 token RAG |
| 并行槽 | 4 | 4 | 4 |
| 4 路端到端聚合吞吐 | 37.51 token/s | 稳态 38.34 token/s | 15.4–15.8 token/s |
| 8 路表现 | 分两批排队，无吞吐增益 | 7/8 成功，出现一次引擎失败 | 分两批排队，吞吐约 16.4 token/s |
| 稳态进程内存 | 约 33.75 GiB | 控制端不可见 | 约 146.9 GiB |
| 视觉输入 | 支持，已测试 | 支持，基础图片已测试 | 不支持 |
| 推荐生产并发 | 4 | 4 | 4 |

综合结论：

- **Qwen3.6-27B 现作为大学物理智能助教的对话与图片识别模型**：4 路持续测试稳定，长上下文与基础视觉测试通过；上线后仍需继续验证真实物理题图质量。
- **Qwen3-VL-30B 保留为已验证的回退模型**：已经覆盖图片识题，性能与现有业务匹配。
- **DeepSeek V4 Flash 可作为纯文本推理补充模型**：能够稳定承载 4 路并发，但冷启动、内存和长提示词首字延迟明显更高。
- 三个模型公开的并行槽均为 4。Qwen3.6 在 8 路测试中已出现一次真实失败，其余模型在 8 路下也只有排队而没有有效吞吐增益。
- Qwen3-VL 与 DeepSeek 本次实际加载上下文均为 8192 token；Qwen3.6 配置为 262144 token，但本次只验证到 16854 token，不能把配置值视为完整业务验收结果。

## 2. 数据口径

- **配置数据**：来自 LM Studio 模型元数据、`lms ps` 和后端进程参数。
- **Qwen3-VL 性能数据**：来自原始客户端 JSONL，并与 LM Studio/llama.cpp 的 `prompt eval time`、`eval time` 和模型标签交叉复核。
- **Qwen3.6 性能数据**：来自流式客户端计时、usage 和 LM Link 服务日志；请求体与服务标签均明确属于 `qwen/qwen3.6-27b`。
- **DeepSeek 性能数据**：由测试客户端通过流式 `/v1/chat/completions` 记录，包括 TTFT、总耗时、成功率和 token 使用量；同时结合 llama.cpp 服务计时校验。
- **TTFT**：请求发出到收到首个流式 token 的时间。对于带内部推理通道的模型，另记录面向用户的“正文首字延迟”。
- **解码吞吐**：只计算输出 token 的生成速度。
- **端到端聚合吞吐**：同一批次所有输出 token 数除以批次总耗时，包含提示词处理、排队和解码。

三个模型都保留了客户端计时，但测试提示词内容并不完全相同。固定输出的并发数据可用于部署参考，不能据此直接比较回答质量。LM Link 可能按块转发 Qwen3.6 的流式数据，因此该模型优先采用完整批次的端到端吞吐，而不使用个别异常的瞬时解码速度。

## 3. 测试环境

| 项目 | Qwen3-VL / DeepSeek 所在服务器 | Qwen3.6 承载设备 |
|---|---|---|
| 设备 | 本机 `node01` | LM Link 设备 `tianwen` |
| 操作系统 | Rocky Linux 10 | 控制端不可见 |
| CPU | 2 × AMD EPYC 9V94，共 256 个在线 CPU 核 | 控制端不可见 |
| 内存 | 565 GiB RAM，63 GiB Swap | 控制端不可见 |
| 计算 GPU | 无；仅有 ASPEED 管理显卡 | 控制端不可见 |
| 推理入口 | Nginx `:1234` → LM Studio / LM Link | 通过 LM Link 接收请求 |
| 并行槽 | 4 | 4 |

`node01` 的 CPU、内存和后端版本不能作为 `tianwen` 的硬件证据。Qwen3.6 的 GPU offload、KV cache、底层运行时和资源占用需要登录 `tianwen` 本机才能确认。

`node01` CPU 支持 AVX-512。初次 DeepSeek 测试使用 LM Studio 的 AVX2 后端；2026-08-10 已用独立原生 AVX-512 后端复测，结果见第 11 节。NUMA 绑定仍未测试。

## 4. Qwen3-VL-30B 测试报告

### 4.1 模型配置

| 项目 | 结果 |
|---|---|
| 模型标识 | `qwen/qwen3-vl-30b` |
| 模型文件 | `Qwen3-VL-30B-A3B-Instruct-Q4_K_M.gguf` |
| 主模型大小 | 18,556,687,200 字节 |
| 视觉投影大小 | 1,083,499,584 字节 |
| 量化 | Q4_K_M |
| 实际上下文 | 8192 token |
| 并行槽 | 4 |
| 推理线程 | 96 |
| 视觉能力 | 图片、视频；不支持音频 |

本次服务虽然按 4 个槽运行，但每个槽的 `n_ctx` 均为 8192，并不是 4 个槽共同平分 8192。

### 4.2 固定 128-token 并发测试

| 并发 | 成功率 | TTFT p50 / max | 总耗时 p50 / max | 单请求解码 | 端到端聚合吞吐 |
|---:|---:|---:|---:|---:|---:|
| 1 | 1/1 | 0.456 / 0.456 秒 | 4.416 / 4.416 秒 | 32.32 token/s | 28.98 token/s |
| 2 | 2/2 | 0.458 / 0.458 秒 | 10.721 / 10.723 秒 | 12.47 token/s | 23.87 token/s |
| 4 | 4/4 | 0.908 / 0.910 秒 | 13.645 / 13.650 秒 | 10.05 token/s | 37.51 token/s |
| 8 | 8/8 | 7.574 / 14.252 秒 | 20.030 / 26.412 秒 | 10.29 token/s（p50） | 38.76 token/s |

观察结果：

- 单请求速度最快，但服务器总吞吐未充分利用。
- 4 路并发时端到端聚合吞吐达到 37.51 token/s、解码聚合约 40.2 token/s，是当前配置下较好的性能/延迟平衡点。
- 8 路并发没有超过 4 路吞吐，只把后 4 个请求排到第二批，尾部时延约翻倍。

### 4.3 持续 4 路稳定性

持续执行 20 个请求（5 批“4 路并发 × 每路 64 token”）：

- 20/20 请求成功，合计输出 1280 token；
- 总测试墙钟时间 34.35 秒，端到端聚合吞吐 37.26 token/s；
- TTFT p50 / p95 / max：1.153 / 1.171 / 1.172 秒；
- 单请求总耗时 p50 / p95 / max：6.929 / 6.946 / 6.952 秒；
- 单批解码时间约 5.77–5.90 秒，解码聚合吞吐约 43.4–44.4 token/s；
- 各轮没有出现明显持续衰减、HTTP 错误或请求卡死。

### 4.4 RAG 长提示词

单路 RAG 型请求客户端计数为 3861 个输入 token、256 个输出 token；llama.cpp 实际提示词处理计数为 3858 token：

| 阶段 | 时间 | 吞吐 |
|---|---:|---:|
| 客户端 TTFT | 22.621 秒 | — |
| 提示词处理 | 22.013 秒 | 175.26 token/s |
| 输出解码 | 11.996 秒 | 21.34 token/s |
| 客户端总耗时 | 34.618 秒 | — |

Qwen 对 3K–4K 级 RAG 输入的预填充速度明显快于 DeepSeek V4 Flash，适合当前“本地知识库检索 + 流式讲解”工作流。

### 4.5 上下文限制

长上下文阶梯测试记录如下：

| 输入规模 | 客户端耗时 | 说明 |
|---:|---:|---|
| 1013 token | 5.753 秒 | 成功 |
| 4013 token | 26.583 秒 | 成功 |
| 7013 token | 34.494 秒 | 成功 |
| 7913 token | 11.879 秒 | 成功，但命中前缀缓存，不能与前三项直接比较 |
| 8213 token | HTTP 400 | 超过实际上下文 |

服务日志实际出现两次错误：

```text
request (8213 tokens) exceeds the available context size (8192 tokens)
```

因此，本次部署的真实上限是 8192 token。模型元数据或应用配置中的更大窗口不能替代实际加载参数。

### 4.6 资源占用

- 模型进程稳态 RSS 约 33.75 GiB；
- 4 路负载期间进程约使用 37 个 CPU 核；
- 系统内存余量充足，没有观察到模型导致的新增 Swap 压力；
- 视觉能力会额外使用视觉投影模型，但仍显著小于 DeepSeek V4 Flash 的内存需求。

首次冷加载约 20 秒；热态短请求 TTFT 通常约 0.2–0.5 秒。视觉请求实测 TTFT 约 4.88 秒、总耗时约 5.3 秒，测试图片内容识别正确。

### 4.7 Qwen 生产建议

- 生产并发设为 **4**；超过 4 的请求进入应用队列。
- 应用上下文预算按 **8192 token** 设置，并为回答预留输出空间。
- 推荐输出上限约 **1024–2048 token**；长回答可继续流式生成，但应避免单次无边界输出。
- 继续作为图片识题、公式识别、图文混合问答和普通 RAG 问答的主模型。

## 5. Qwen3.6-27B 测试报告

### 5.1 模型状态

| 项目 | 结果 |
|---|---|
| 模型标识 | `qwen/qwen3.6-27b` |
| 运行设备 | LM Link 设备 `tianwen` |
| 架构 | `qwen35` |
| 量化 | Q8_0 |
| 模型文件规模 | 29,527,097,855 字节（约 27.50 GiB） |
| 配置上下文 | 262144 token |
| 并行槽 | 4 |
| 视觉能力 | 已声明支持；基础图片实测通过 |
| Tool-use | 模型元数据声明支持 |
| TTL | 无，模型常驻 |
| 测试结束状态 | `idle`，排队数 0 |

控制端只能确认模型由 `tianwen` 承载，无法读取该设备的 CPU、GPU、内存、GPU offload、KV cache 和底层运行时参数。

### 5.2 接口兼容性

| 测试 | 结果 |
|---|---|
| 普通文本请求 | 正常，准确返回指定文本 |
| SSE 流式输出 | 正常，能够返回 usage 与结束标记 |
| `reasoning_effort: none` | 正常，未消耗 reasoning token |
| `enable_search: true` | HTTP 200，返回指定文本 |
| 图片 `data:` URI 输入 | 正常 |

`enable_search: true` 返回 200 只证明当前接口接受该字段，不能证明模型实际执行了互联网搜索。

### 5.3 固定 128-token 并发测试

每个请求要求持续生成大学物理说明文字，所有请求通过项目实际使用的 `:1234/v1/chat/completions` 流式入口发送。

| 并发 | 成功率 | TTFT p50 | 总耗时 p50 | 批次墙钟 | 成功输出 | 端到端聚合吞吐 |
|---:|---:|---:|---:|---:|---:|---:|
| 1 | 1/1 | 1.094 秒 | 3.783 秒 | 3.785 秒 | 128 token | 33.82 token/s |
| 2 | 2/2 | 4.190 秒 | 6.730 秒 | 6.790 秒 | 256 token | 37.70 token/s |
| 4 | 4/4 | 4.070 秒 | 6.959 秒 | 7.007 秒 | 512 token | 73.07 token/s |
| 8 | 7/8 | 8.102 秒 | 12.543 秒 | 16.717 秒 | 896 token | 53.60 token/s |

4 路测试全部成功。8 路时后 4 个请求需要排队，成功请求的 TTFT p95 约 14.46 秒、总耗时 p95 约 16.68 秒；服务日志同时出现一次 `Engine protocol predict request failed: fetch failed`。因此 8 路不能作为生产并发值。

### 5.4 持续 4 路稳定性

连续执行 5 批，每批 4 路并发、每路 64 个输出 token：

| 批次 | 成功率 | 批次墙钟 | TTFT p50 | 总耗时 p50 | 聚合吞吐 |
|---:|---:|---:|---:|---:|---:|
| 1 | 4/4 | 6.799 秒 | 5.246 秒 | 6.695 秒 | 37.65 token/s |
| 2 | 4/4 | 6.682 秒 | 5.050 秒 | 6.607 秒 | 38.31 token/s |
| 3 | 4/4 | 6.762 秒 | 5.137 秒 | 6.719 秒 | 37.86 token/s |
| 4 | 4/4 | 6.569 秒 | 4.960 秒 | 6.529 秒 | 38.97 token/s |
| 5 | 4/4 | 6.572 秒 | 5.295 秒 | 6.482 秒 | 38.96 token/s |

合计 20/20 成功，输出 1280 token，总墙钟约 33.38 秒，端到端聚合吞吐约 **38.34 token/s**。服务端记录 20 条开始和 20 条正常结束，没有 ERROR/WARN；结束后模型恢复 `idle`。

### 5.5 RAG 型请求

单路请求使用 3202 个输入 token，并生成 256 个输出 token：

| 指标 | 结果 |
|---|---:|
| 输入 | 3202 token |
| 输出 | 256 token |
| TTFT | 1.687 秒 |
| 总耗时 | 6.625 秒 |
| 近似解码速度 | 51.63 token/s |
| 端到端输出吞吐 | 38.62 token/s |

同类请求执行 2 路并发时，2/2 成功，TTFT p50 4.114 秒，总耗时 p50 10.173 秒，端到端聚合吞吐 50.11 token/s。

### 5.6 长上下文检索

测试把唯一随机标记放在长篇大学物理文本中部，要求模型只返回该标记：

| 输入 token | 标记位置 | 结果 | TTFT | 总耗时 |
|---:|---|---|---:|---:|
| 4857 | 中部 | 准确找回 | 3.578 秒 | 4.683 秒 |
| 16854 | 中部 | 准确找回 | 6.246 秒 | 6.475 秒 |

这证明当前实例至少能够正确处理约 16.9K token 的单路输入。262144 是实例公开的配置值，但本次没有执行接近极限的 262K 测试，也无法从控制端确认 4 路并发时底层 KV cache 的实际分配方式。

### 5.7 视觉测试

测试图片为左半红色、右半蓝色的确定性 PNG，以项目相同的 base64 `data:image/png` 方式发送。

| 指标 | 结果 |
|---|---:|
| 输入 | 47 token + 1 张 128×64 PNG |
| 输出 | `左=红色，右=蓝色` |
| 正确性 | 通过 |
| TTFT | 2.912 秒 |
| 总耗时 | 3.050 秒 |

该结果只验证基本视觉链路。项目切换到 Qwen3.6 后，仍应使用真实大学物理题图继续测试 OCR、公式、受力图、电路图和低清扫描件。

### 5.8 Qwen3.6 生产建议

- 生产并发限制为 **4**，第 5 个及之后的请求进入有界队列。
- 不使用 8 路直冲模型；本次已经出现真实失败，而不只是尾部延迟增加。
- 先以 16K–32K 应用上下文进行业务验证，再逐步扩大；不要仅因实例显示 262144 就直接发送超长历史。
- 项目已按部署决策切换到 Qwen3.6；上线后持续进行真实物理题集 A/B 测试，异常时回退到 Qwen3-VL。
- 若要评估资源利用率、显存/RAM、KV cache 和后端优化，必须在 `tianwen` 本机采样。

## 6. DeepSeek V4 Flash 测试报告

### 6.1 模型配置

| 项目 | 结果 |
|---|---|
| 模型标识 | `deepseek/deepseek-v4-flash` |
| 架构 | DeepSeek4，`256x8.4B` MoE |
| 模型文件规模 | 156.38 GB（145.64 GiB） |
| 量化 | MXFP4 |
| 厂商/元数据最大上下文 | 1,048,576 token |
| 本次实际上下文 | 8192 token |
| 并行槽 | 4 |
| 推理线程 | 96 |
| 视觉能力 | 不支持 |
| Tool-use 元数据 | 未声明支持 |

### 6.2 冷启动

第一次通过 Nginx 触发按需加载时：

- 服务开始读取约 156 GB 权重；
- Nginx 在约 60 秒返回 `504 Gateway Time-out`；
- 客户端断开后，LM Studio 中止了该次启动，模型回到未加载状态。

随后使用服务器本机命令显式加载：

```bash
lms load deepseek/deepseek-v4-flash \
  --gpu off \
  --context-length 8192 \
  --parallel 4 \
  --ttl 1800
```

显式加载成功，用时 **112.07 秒**。这说明生产环境不应依赖普通 HTTP 请求完成冷启动，应在服务启动阶段预加载模型，或者提高代理超时并向前端明确显示加载状态。

### 6.3 固定 128-token 并发测试

正式测试共 23 个请求，23/23 成功，无 HTTP 错误和流式断开。

| 并发 | 请求数 | TTFT p50 / p95 | 总耗时 p50 / p95 | 单请求解码 p50 | 端到端聚合吞吐 |
|---:|---:|---:|---:|---:|---:|
| 1 | 3 | 1.47 / 1.71 秒 | 20.34 / 20.37 秒 | 6.79 token/s | 约 6.3 token/s |
| 2 | 4 | 2.81 / 2.84 秒 | 28.75 / 29.89 秒 | 4.94 token/s | 8.6–9.3 token/s |
| 4 | 8 | 4.85 / 5.08 秒 | 32.82 / 33.21 秒 | 4.58 token/s | 15.4–15.8 token/s |
| 8 | 8 | 20.02 / 35.67 秒 | 46.93 / 62.45 秒 | 4.76 token/s | 约 16.4 token/s |

观察结果：

- 4 路并发时解码聚合吞吐约为 18.3 token/s，端到端聚合吞吐约为 15.4–15.8 token/s。
- 8 路并发时，服务器明确显示 4 个请求执行、4 个请求排队。
- 8 路相较 4 路的端到端吞吐只增加约 4%–6%，但第二批请求的 TTFT 上升到约 35.7 秒。
- 因此推荐生产并发为 **4**，不建议依靠 8 路客户端并发提高吞吐。

### 6.4 大学物理回答

测试问题要求解释动量守恒、适用条件、公式、碰撞例子和易错点：

| 指标 | 结果 |
|---|---:|
| 输入 | 37 token |
| 输出 | 370 token |
| 首个推理 token | 1.315 秒 |
| 面向用户的正文首字 | 5.432 秒 |
| 总耗时 | 55.007 秒 |
| 平均输出速度 | 6.89 token/s |
| 完成状态 | `stop`，正常完成 |

### 6.5 内部推理 token

DeepSeek 会先通过 `reasoning_content` 输出内部推理，再生成 `content` 正文：

- 简单“只回复测试成功”请求：16 个输出 token，正文首字约 2.85 秒，总耗时约 3.14 秒；
- 长写作任务若只允许 128 个输出 token，可能在内部推理尚未结束时触发 `length`，最终 `content` 为空；
- 测试 `enable_search=true` 时接口返回 HTTP 200，但 32-token 上限仍被推理内容耗尽。这只能证明字段被接口接受，不能证明模型实际具备联网搜索能力。

应用必须把“推理 token + 正文 token”共同计入输出预算，不能把 128 token 当成用户一定能看到的 128 token 正文。

### 6.6 RAG 长提示词

输入 3216 token、要求只回复 `OK`：

| 指标 | 结果 |
|---|---:|
| 提示词处理 | 58.270 秒 |
| 提示词吞吐 | 55.19 token/s |
| 首个推理 token | 58.547 秒 |
| 正文首字 | 60.589 秒 |
| 总耗时 | 60.774 秒 |
| 输出 | 12 token |

这说明 DeepSeek 能接收 3K 级 RAG 输入，但用户需要等待约一分钟才看到正文。若用于交互式助教，应压缩检索片段、减少重复教材内容，并尽量把有效输入控制在约 2000 token 内。

### 6.7 资源占用

| 指标 | 结果 |
|---|---:|
| 稳态/测试峰值 RSS | 约 146.8–146.9 GiB |
| 加载期进程内存高水位 | 约 282.7 GiB |
| 推理峰值 CPU | 约 95 个 CPU 核 |
| C=8 队列 | 4 个执行、4 个排队 |
| 新增 Swap | 未观察到；进程 VmSwap 约 18 MiB |
| 测试结束系统可用内存 | 约 411 GiB |

虽然服务器内存能够承载该模型，但当前 96 个推理线程使 CPU 峰值约为 95 核，其余 CPU 核没有直接转化为更多并发槽。AVX-512 复测已经完成；提高线程数和双 NUMA 节点绑定仍需单独基准测试。

### 6.8 DeepSeek 生产建议

- 并发限制为 **4**，在应用层增加有界队列。
- 实际上下文按 **8192 token** 配置；若需要更大上下文，应重新加载模型并重新测试内存和时延。
- 建议输出上限 **1024–1536 token**。更长输出在约 6–7 token/s 下可能等待数分钟。
- 单次 RAG 输入建议控制在约 **2000 token** 内。
- 应预加载并保持常驻；30 分钟 TTL 会卸载模型，下一次冷启动约需 112 秒。
- 若仍保留 TTL，应提高 Nginx 冷启动超时，或由应用先检测模型状态并显示加载进度。
- 不用于图片识题；图片请求交给当前 Qwen3.6，Qwen3-VL 作为回退。

## 7. 三模型观测结果对比

| 维度 | Qwen3-VL-30B | Qwen3.6-27B | DeepSeek V4 Flash |
|---|---:|---:|---:|
| 执行设备 | `node01` | `tianwen`（LM Link） | `node01` |
| 单路 128-token 端到端吞吐 | 28.98 token/s | 33.82 token/s | 约 6.3 token/s |
| 4 路固定 128-token 单批吞吐 | 37.51 token/s | 73.07 token/s | 15.4–15.8 token/s（AVX2） |
| 4 路持续 64-token 吞吐 | 37.26 token/s | 38.34 token/s | 未做同口径持续测试 |
| 8 路结果 | 8/8，分两批排队 | 7/8，一次引擎失败 | 8/8，分两批排队 |
| 已验证输入规模 | 7913 token | 16854 token | 3216 token RAG |
| 稳态内存 | 约 33.75 GiB | 控制端不可见 | 约 146.9 GiB |
| 视觉能力 | 已测试 | 基础图片已测试 | 不支持 |
| 内部推理通道 | 本次未观察到同类问题 | 关闭推理后正常 | 明确存在，需预留 token |

Qwen3.6 的 73.07 token/s 是一次固定 128-token 批次结果，不是稳定吞吐；其 5 批持续测试结果为 38.34 token/s。固定测试与持续测试的输出长度不同，同时可能受前缀缓存、模型状态和 LM Link 分块转发影响。

三模型不在完全相同的硬件和链路上运行，提示词也不完全一致。本表只能比较本次部署链路的观测结果，不能据此归因模型架构优劣，也不评价回答质量。

## 8. 推荐部署策略

### 8.1 模型路由

- 对话、图片、截图、实验图、受力图和公式照片统一交给 `tianwen` 上的 `qwen/qwen3.6-27b`。
- `qwen/qwen3-vl-30b` 作为已验证的回退模型保留；真实物理题图 A/B 测试持续进行。
- 需要额外文本推理且能够接受更长等待时，可选择 `deepseek/deepseek-v4-flash`。
- DeepSeek 原生 AVX-512 服务当前只监听 `127.0.0.1:1236`，尚未替换生产 `/v1` 入口。

### 8.2 并发和排队

- 每个模型独立设置最大并发 4，尤其禁止向 Qwen3.6 直接发起 8 路压力。
- 超过 4 的请求进入有界队列，并在前端显示“正在排队”。
- 不要让前端无限重试；重试会放大排队和模型负载。

### 8.3 上下文和输出

- Qwen3-VL 与 DeepSeek 的应用上下文预算必须与实际加载值 8192 对齐。
- Qwen3.6 先按 16K–32K 业务预算逐级验证，不直接按公开的 262144 token 配置值发送超长历史。
- RAG 片段应去重、压缩，并优先保留与当前问题最相关的教材段落。
- 给系统提示、历史、检索资料、图片描述和输出分别预留预算。
- DeepSeek 的输出预算必须包含内部推理 token。

### 8.4 冷启动和超时

- 区分连接超时、首 token 超时和流式空闲超时。
- DeepSeek 使用前显式预加载；普通 Nginx 60 秒超时不足以覆盖 112 秒冷启动。
- 生产环境可取消短 TTL，或定时进行轻量健康请求保持模型常驻。
- 前端应显示“模型正在加载”，不能只显示无限旋转动画。

## 9. 测试限制

1. 三模型并非运行在同一设备：Qwen3-VL 与 DeepSeek 在 `node01`，Qwen3.6 经 LM Link 在 `tianwen`；跨设备结果不能归因为模型本身。
2. 固定并发测试的提示词不完全相同；固定测试使用 128-token 输出，部分持续测试使用 64-token 输出，绝对延迟和吞吐只用于部署参考。
3. LM Link 可能缓冲或分块转发 Qwen3.6 的流式数据，个别请求的瞬时解码速度不可靠。
4. `tianwen` 的 CPU、GPU、内存、offload、KV cache 和运行时版本在控制端不可见，无法报告 Qwen3.6 的资源效率。
5. Qwen3.6 尚未测试 32K、64K、128K、262K 极限上下文，也未测试复杂物理题图、多图、视频和工具调用。
6. 测试为短时基准，没有覆盖 30–60 分钟持续压力、多实例负载均衡或长期温度与频率变化。
7. 承载能力不等于回答质量；物理正确率、公式规范性、幻觉率和知识库引用质量需要另行评测。
8. DeepSeek AVX-512 后端尚未接入生产入口，也未完成长期稳定性和 NUMA 优化验证。

## 10. 后续建议

- 使用完全相同的提示词、输入长度和输出上限，对三模型做跨链路对照测试；
- 为三模型统一增加 512/1024-token 输出和 2K/4K/8K 输入矩阵；
- 连续运行 30–60 分钟，记录 p95/p99、错误率、温度和吞吐衰减；
- 用真实大学物理题集比较 Qwen3-VL 与 Qwen3.6 的 OCR、公式、受力图和电路图正确率；
- 在 `tianwen` 本机采集 Qwen3.6 的硬件、KV cache、内存和后端信息；
- 单独评估 DeepSeek 的大学物理推理质量与内部推理 token 成本；
- 继续测试 DeepSeek 的线程数和双 NUMA 节点绑定方案。

## 11. 附录：DeepSeek AVX-512 复测

### 11.1 后端与启动配置

| 项目 | 结果 |
|---|---|
| 源码 | 官方 `llama.cpp`，固定提交 `74ce15741b420b8d6f12e720398458b576c51c2c` |
| 编译器 | GCC 14.3.1 |
| 编译方式 | Release，`GGML_NATIVE=ON`，纯 CPU |
| AVX-512 验证 | 编译参数含 `-march=native`；`libggml-cpu.so` 反汇编确认存在 ZMM/AVX-512 指令 |
| 模型 | DeepSeek-V4-Flash-0731 MXFP4，4 个 GGUF 分片 |
| 服务地址 | `127.0.0.1:1236`，仅服务器本机可访问 |
| 上下文 | 8192 token，统一 KV 缓冲 |
| 并行槽 | 4 |
| 推理线程 | 96 |
| batch / ubatch | 2048 / 512 |
| 冷加载 | 约 92.57 秒 |
| 稳态 RSS | 约 146.6 GiB |

服务日志明确显示：`n_slots = 4`、`n_ctx_slot = 8192`、`kv_unified = true`。四个槽都能够使用完整的 8192 token 上下文，而不是每槽只有 2048 token。

### 11.2 固定 128-token 对比

AVX-512 使用统一提示词进行 1、2、4 路各一批测试，并额外重复一批 4 路测试。共 11 个请求，11/11 成功，无 HTTP 错误或流式断开。

| 并发 | 后端 | TTFT | 总耗时 | 单请求解码 | 端到端聚合吞吐 |
|---:|---|---:|---:|---:|---:|
| 1 | LM Studio AVX2 | 约 1.47 秒 | 约 20.34 秒 | 6.79 token/s | 约 6.3 token/s |
| 1 | 原生 AVX-512 | 1.322 秒 | 19.031 秒 | 7.172 token/s | 6.725 token/s |
| 2 | LM Studio AVX2 | 约 2.81 秒 | 约 28.75 秒 | 4.94 token/s | 8.6–9.3 token/s |
| 2 | 原生 AVX-512 | 1.841 秒 | 22.341 秒 | 6.195 token/s | 11.458 token/s |
| 4 | LM Studio AVX2 | 约 4.85 秒 | 约 32.82 秒 | 4.58 token/s | 15.4–15.8 token/s |
| 4 | 原生 AVX-512，第 1 批 | 2.711 秒 | 24.743 秒 | 5.764 token/s | 20.691 token/s |
| 4 | 原生 AVX-512，第 2 批 | 2.912 秒 | 26.256 秒 | 5.440 token/s | 19.497 token/s |

主要结论：

- 单路解码从约 6.79 提升到 7.17 token/s，提升约 **5.6%**；
- 4 路两批平均聚合吞吐约 **20.09 token/s**，相对 AVX2 中心值约 15.6 token/s，提升约 **28.8%**；
- 4 路平均总耗时约 **25.50 秒**，相对 AVX2 的约 32.82 秒下降约 **22.3%**；
- 4 路平均 TTFT 约 **2.81 秒**，相对 AVX2 的约 4.85 秒下降约 **42.0%**；
- AVX-512 的主要收益体现在并发吞吐和首 token 延迟，单路提升较小，说明大模型推理仍明显受内存带宽影响；
- 模型内存占用基本没有变化，仍约 146–147 GiB。

### 11.3 回答与思考模式

默认自动思考模式会先把 token 用于 `reasoning_content`。简单物理题在 256 token 上限下可能尚未进入正文，因此不能把“正文为空”误判为服务失败。请求增加 `"reasoning_effort": "none"` 后，模型能直接给出正确公式与结果。例如质量 2 kg、速度从 3 m/s 增至 5 m/s 的题目正确得到动能增加 16 J。

用于交互式助教时建议：

- 普通讲解请求默认关闭长思考，或设置明确的思考 token 预算；
- 需要复杂推理时再开启思考，并把内部推理 token 纳入输出预算；
- 生产并发仍设为 4；超过 4 的请求在应用层排队；
- 当前 AVX-512 服务只监听 `127.0.0.1:1236`，尚未替换现有 `/v1` 入口，避免影响 Qwen 和其他 LM Studio 模型。

---

本报告不包含 API 密钥，所有吞吐数据均来自实际模型服务请求或去敏后的服务日志。
