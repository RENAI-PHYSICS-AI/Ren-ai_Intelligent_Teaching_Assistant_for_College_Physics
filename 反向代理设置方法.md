# 反向代理设置方法

本文说明如何将大学物理智能助教部署在反向代理的 `/agent/` 子路径下，并正确转发网页、管理员页面、模型接口和 Paraformer 语音 WebSocket。

## 一、推荐访问结构

```text
浏览器
  └─ https://服务器地址:1234/agent/
       └─ 反向代理 → 127.0.0.1:8501（统一网关）
                          ├─ Streamlit → 127.0.0.1:8502
                          ├─ 管理员 API → 127.0.0.1:8603
                          ├─ Paraformer → 127.0.0.1:8604
                          └─ 可视化实验 → 9384/9385/9386/9387
```

内部端口不应直接暴露到局域网。

## 二、Nginx HTTP 反向代理（启用 TLS 前的参考）

以下示例假定外部入口为 `1234`，应用公共前缀为 `/agent`：

```nginx
server {
    listen 1234;
    server_name _;

    location /v1/ {
        proxy_pass http://127.0.0.1:1235/v1/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Connection "";
        # 首次加载 128K 多模态模型、处理高分辨率图片时可能超过 60 秒。
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }

    location /agent/ {
        proxy_pass http://127.0.0.1:8501/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
        proxy_buffering off;
    }
}
```

`proxy_pass` 末尾的 `/` 很重要：它会把外部的 `/agent/` 去掉后交给内部网关。网关通过 `PHYSICS_GATEWAY_PUBLIC_PREFIX=/agent` 和 `X-Forwarded-Prefix` 恢复公开路径，因此管理员跳转会保持为 `/agent/analytics`。

检查配置并重载：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

## 三、项目配置

编辑用户目录中的配置文件：

```bash
cd ~/agent_of_college_physics
```

当前生产入口配置：

```ini
PHYSICS_PUBLIC_BASE_URL=https://服务器地址:1234/agent
PHYSICS_GATEWAY_PUBLIC_PREFIX=/agent
PHYSICS_BASE_URL=http://127.0.0.1:1235/v1
PHYSICS_MODEL=mimo-vl-local-prod
PHYSICS_VISION_MODEL=mimo-vl-local-prod
```

修改后重启：

```bash
bash manage.sh restart
bash manage.sh check
```

## 四、必须转发的路径

| 外部路径 | 内部用途 |
|---|---|
| `/agent/` | 主页面 |
| `/agent/_stcore/` | Streamlit 前端与流式连接 |
| `/agent/admin-login` | 管理员登录票据验证 |
| `/agent/session-login` | 注册用户登录会话换取 |
| `/agent/session-logout` | 注册用户退出并清除会话 |
| `/agent/analytics` | 管理员数据页面 |
| `/agent/identity-roster` | 身份名册接口 |
| `/agent/asr/ws` | Paraformer 语音 WebSocket |
| `/agent/asr/health` | 语音服务健康检查 |
| `/agent/experiments/lissajous/` | 李萨如实验 |
| `/agent/experiments/sound-speed/` | 声速实验 |
| `/agent/experiments/electron-em/` | 电子荷质比实验 |
| `/agent/experiments/photoelectric/` | 光电效应实验 |

管理员页面中的 JavaScript 请求也必须保留 `/agent` 前缀，否则会出现 Nginx 404。

## 五、仅使用 1234 时启用 HTTPS

同一个端口不能同时监听 HTTP 和 HTTPS。若只能开放 `1234`，需要将 Nginx 的：

```nginx
listen 1234;
```

改为：

```nginx
listen 1234 ssl;
ssl_certificate /项目目录/config/tls/server.crt;
ssl_certificate_key /项目目录/config/tls/server.key;
ssl_protocols TLSv1.2 TLSv1.3;
```

同时保留 `/v1/` 和 `/agent/` 两个 location。两个 location 都应设置较长的读写超时；其中 `/v1/` 还应关闭响应缓冲，以避免首次加载 128K 多模态模型或处理图片时触发 Nginx 默认超时：

```nginx
proxy_set_header X-Forwarded-Proto https;
proxy_set_header X-Forwarded-Host $http_host;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
proxy_buffering off;
```

配置文件改为：

```ini
PHYSICS_PUBLIC_BASE_URL=https://服务器地址:1234/agent
PHYSICS_BASE_URL=http://127.0.0.1:1235/v1
```

模型调用建议走服务器本机的 `127.0.0.1:1235`，避免应用访问自身 HTTPS 证书时产生校验问题。

切换前必须备份 Nginx 配置，并使用 root 执行：

```bash
sudo nginx -t
sudo systemctl reload nginx
```

浏览器访问：

```text
https://服务器地址:1234/agent/
```

首次使用需要将 `config/tls/physics-assistant-ca.crt` 导入客户端的受信任根证书。只复制 `.crt` 文件，绝不能分发 CA 私钥或服务器私钥。

## 六、验证命令

```bash
curl -i http://127.0.0.1:8501/_stcore/health
curl -i http://127.0.0.1:8603/health
curl -i http://127.0.0.1:8604/health
curl -i http://127.0.0.1:1235/v1/models
curl --cacert config/tls/physics-assistant-ca.crt \
  https://服务器地址:1234/agent/_stcore/health
curl --cacert config/tls/physics-assistant-ca.crt \
  https://服务器地址:1234/agent/asr/health
curl --cacert config/tls/physics-assistant-ca.crt \
  https://服务器地址:1234/v1/models
```

## 七、常见问题

- `/v1/` 返回 `Unexpected endpoint or method`：这是正常的，模型列表应访问 `/v1/models`。
- 管理员跳转到根路径：检查 `PHYSICS_GATEWAY_PUBLIC_PREFIX=/agent` 和 `X-Forwarded-Prefix`。
- 页面加载但语音卡住：检查 `/agent/asr/ws` 的 WebSocket Upgrade、HTTPS/WSS 和证书信任。
- HTTP 页面无法录音：这是浏览器安全策略。正式方案使用 HTTPS；受控局域网临时方案才使用 Edge 的安全来源例外。
- 8443 无法访问：不要把内部端口 8604 暴露出去；如果只能使用 1234，应在 1234 上终止 TLS。
