# `tjracphy` 服务器硬件与运行状态报告

> 采样时间：2026-09-11 20:50–20:54 CST
> 采样方式：通过 SSH 对 Rocky 服务器执行只读系统查询
> 说明：硬件规格与实时运行状态分开记录；温度、负载、内存占用和磁盘使用率仅代表采样时刻。

## 1. 摘要

| 项目 | 已确认信息 |
| --- | --- |
| 主机 | Giga Computing `MZ73-LM1-000` 双路裸机服务器 |
| CPU | 2 × AMD EPYC 9V94 128-Core，共 256 个物理核心；当前未启用 SMT |
| NUMA | 2 个节点：NUMA 0 对应 CPU 0–127，NUMA 1 对应 CPU 128–255 |
| 内存 | OS 可用约 565.98 GiB，Registered DDR5 ECC，SECDED；两节点容量基本均衡 |
| 计算指令集 | AVX2、AVX-512F/BW/VL/VNNI/BF16/VBMI 等 |
| 计算 GPU | 无；ASPEED 显卡仅用于服务器管理显示，不能承担大模型推理 |
| 系统盘 | Intel `SSDPE2KX020T8` 2 TB NVMe |
| 数据盘 | Seagate `ST8000NM0055-1RM112` 8 TB SATA HDD |
| 网络 | Broadcom BCM57416 双口 10GbE/RDMA；当前有效链路仅协商到 1Gbps |
| 操作系统 | Rocky Linux 10.2，Linux `6.12.0-55.41.1.el10_0.x86_64` |
| 当前模型 | MiMo VL 位于 NUMA 0；DeepSeek V4 Flash 位于 NUMA 1；两者健康检查正常 |

当前硬件很适合大内存、纯 CPU 的量化模型推理。主要优势是核心数、内存容量、AVX-512 和双 NUMA 节点；主要短板是没有计算 GPU，以及 10GbE 网卡当前只运行在 1Gbps。

## 2. 整机、主板与固件

| 项目 | 值 |
| --- | --- |
| 厂商 | Giga Computing |
| 整机/主板型号 | `MZ73-LM1-000` |
| Product version | `0100` |
| Board version | `03020302` |
| 机箱类型 | DMI type 17，Server Chassis |
| BIOS 厂商 | GIGABYTE |
| BIOS 版本 | `R15_F39` |
| BIOS 日期 | 2025-02-27 |
| 虚拟化环境 | `none`，确认为裸机而非虚拟机 |

板卡版本编码不足以可靠映射到官网的具体 revision，因此本报告不据此推断主板修订版。厂商页面表明 MZ73-LM1 平台具有 24 个 DDR5 RDIMM 插槽、每 CPU 12 个内存通道；这是平台能力，不代表当前实际插条数量或速度：[GIGABYTE MZ73-LM1 产品页](https://www.gigabyte.com/us/Enterprise/Server-Motherboard/MZ73-LM1-rev-3x)。

## 3. CPU、缓存与 NUMA

### 3.1 CPU 拓扑

- 处理器：2 × `AMD EPYC 9V94 128-Core Processor`。
- 每插槽 128 核，共 256 个在线 CPU。
- `Thread(s) per core = 1`，当前 OS 拓扑没有 SMT；不能把这台服务器写成 512 线程。
- 最大报告频率约 3.677 GHz，最低频率 1.5 GHz，Boost 已启用。
- 调频策略：`schedutil`；采样时全机平均约 1.68–1.85 GHz，符合低负载状态。
- CPU 微码：`0xaa00116`。

### 3.2 缓存

| 层级 | 总量与组织 |
| --- | --- |
| L1d | 8 MiB，共 256 组，每核 32 KiB |
| L1i | 8 MiB，共 256 组，每核 32 KiB |
| L2 | 256 MiB，共 256 组，每核 1 MiB |
| L3 | 512 MiB，共 32 组，每组 16 MiB |

### 3.3 NUMA

| NUMA 节点 | CPU | OS 可见内存 |
| --- | --- | ---: |
| 0 | 0–127 | 约 282.59 GiB |
| 1 | 128–255 | 约 283.40 GiB |

NUMA 距离矩阵中本地访问为 10、跨节点访问为 32，跨节点内存访问代价明显。对大模型采用“一个模型一个 NUMA 节点”的方式是合理的。

### 3.4 关键指令集

已确认支持：

- AVX、AVX2、FMA；
- AVX-512F、DQ、IFMA、CD、BW、VL、VBMI、VBMI2、VNNI、BITALG、VPOPCNTDQ、BF16；
- AES/VAES、SHA-NI、PCLMUL/VPCLMUL、GFNI；
- AMD-V/SVM/NPT。

因此 DeepSeek 使用 AVX-512 后端符合硬件能力。MiMo 当前仍运行在 LM Studio 的 AVX2 后端，存在通过兼容性验证与基准测试后迁移至 AVX-512 后端的优化空间。

## 4. 内存

| 项目 | 状态 |
| --- | --- |
| `/proc/meminfo` 可见物理内存 | 593,478,484 KiB，约 565.98 GiB |
| 采样时已用 | 约 201–202 GiB |
| 采样时可用 | 约 363–364 GiB |
| Swap | 64 GiB，总使用约 930 MiB |
| 类型 | Registered DDR5 ECC |
| ECC 模式 | SECDED |
| EDAC 当前计数 | CE=0，UE=0 |
| 静态 HugeTLB | 未配置，`HugePages_Total=0` |
| Transparent Huge Pages | `always` |

`lsmem` 的 578 GiB 是按 2 GiB 在线地址块统计的跨度，与 `MemTotal` 的 OS 可用内存口径不同，不应直接写成“已安装 578 GiB”。当前账户无法读取 DMI 内存表或 SPD，故实际 DIMM 数量、单条容量、厂商、料号和配置速度仍待管理员权限确认。

EDAC 的 CE/UE 均为 0，只表示本次开机以来内核当前计数未发现可纠正或不可纠正内存错误，不代表完整历史健康证明。

## 5. 存储

### 5.1 物理设备

| 设备 | 型号 | 原始容量 | 接口/介质 | 主要用途 |
| --- | --- | ---: | --- | --- |
| `/dev/nvme0n1` | Intel `SSDPE2KX020T8`，固件 `VDV10131` | 2.000 TB / 1.819 TiB | NVMe SSD | EFI、`/boot`、根文件系统、64 GiB swap |
| `/dev/sda` | Seagate `ST8000NM0055-1RM112` | 8.002 TB / 7.277 TiB | SATA 7200 rpm HDD | `/home` |

总原始容量约 10.002 TB（9.096 TiB）。未发现 Linux MD 阵列，也未发现挂载中的 LVM、dm-crypt 或其他 device-mapper 数据卷；当前布局表现为 NVMe/SATA 直连。由于 LVM 查询受到非 root 权限限制，此结论限于 OS 当前可见布局。

### 5.2 文件系统使用情况

| 挂载点 | 文件系统 | 容量 | 已用 | 可用 | 使用率 |
| --- | --- | ---: | ---: | ---: | ---: |
| `/` | XFS | 1.8 TiB | 858 GiB | 938 GiB | 48% |
| `/home` | ext4 | 7.3 TiB | 186 GiB | 6.7 TiB | 3% |
| `/boot` | XFS | 769 MiB | 484 MiB | 285 MiB | 63% |
| `/boot/efi` | FAT32 | 1022 MiB | 9 MiB | 1014 MiB | 1% |

当前容量和 inode 均无压力。NVMe 状态为 `live`，但当前 SSH 账户无法读取 NVMe/SATA SMART，因此尚不能确认介质寿命、坏块、通电时长和历史错误。

## 6. 网络

物理网卡为 Broadcom BCM57416 双口 10GbE/RDMA 控制器，驱动 `bnxt_en`，固件 `229.0.141.0/pkg 229.1.123.0`，位于 NUMA 0。

| 接口 | 链路 | 当前协商速率 | 说明 |
| --- | --- | ---: | --- |
| `enp1s0f0np0` | UP，Full Duplex | **1 Gbps** | 当前业务链路 |
| `enp1s0f1np1` | DOWN，无载波 | 未协商 | 第二物理端口 |
| `oray_vnc` | UP | 虚拟接口 | 其报告速率不代表物理吞吐 |

网卡硬件支持 10GbE，但当前有效链路只协商到 1Gbps。可能限制因素包括交换机端口、网线类别、模块/收发器和对端协商配置。

两个物理端口目前配置了相同 IPv4 地址，且未检测到 bond/team。第二端口当前断开所以没有造成冲突；若未来接通，应先改为正式 bonding/teaming，或给两个接口分配不同地址，避免重复路由和 ARP 不确定性。

有效端口累计约 12.8 亿个接收包，RX/TX errors 和 dropped 均为 0，仅有 33 个 missed，未见明显链路质量异常。网卡具备 RDMA 能力，但当前系统没有暴露可用 RDMA 设备。

## 7. 图形与计算加速设备

- 未发现 NVIDIA、AMD Instinct/Radeon、Intel Arc/Flex/Max、NPU 或其他计算加速卡。
- 唯一显示设备为 ASPEED Graphics Family，属于服务器管理/BMC 显示芯片，不能用于大模型推理。
- 当前 MiMo 与 DeepSeek 均为纯 CPU 推理，`n-gpu-layers=0`。

因此这台服务器的推理性能主要受模型量化方式、DDR5 内存带宽、NUMA 局部性和后端实现影响，而不是 GPU 显存或 CUDA/ROCm。

## 8. 温度、ECC 与实时负载

| 项目 | 采样值 |
| --- | --- |
| CPU 0 Tctl | 约 37.8–38.4°C |
| CPU 1 Tctl | 约 34.9–35.5°C |
| CCD 温度 | 约 22.6–27.5°C |
| Uptime | 约 291 天 4 小时 |
| Load average | 约 0.67 / 0.52 / 0.47 |
| 登录用户数 | 7 |
| EDAC CE/UE | 0 / 0 |

对 256 核服务器而言，采样时负载极低，CPU 温度正常。服务器已连续运行约 291 天，建议在维护窗口检查待重启更新、固件和硬件日志，但本报告不据此断言存在故障。

## 9. 本地大模型实际占用

### 9.1 MiMo VL Miloco 7B

- 模型：Q4_0，视觉投影为 BF16。
- 上下文：128,000。
- 并行槽：4。
- 线程：128。
- 后端：LM Studio llama.cpp AVX2 2.31.2。
- CPU affinity：0–127。
- RSS：约 23.63 GiB。
- 实际内存分布：NUMA 0 约 24,206.86 MiB，NUMA 1 约 13.74 MiB，约 99.94% 位于 NUMA 0。
- 健康状态：`127.0.0.1:1237/health` 正常。

### 9.2 DeepSeek V4 Flash

- 模型：MXFP4。
- 上下文：1,048,576。
- 并行槽：1。
- 线程：128。
- 后端：自编译 llama.cpp AVX-512，DIO load。
- CPU affinity：128–255。
- RSS：约 153.53 GiB。
- 实际内存分布：NUMA 1 约 157,223.51 MiB，NUMA 0 约 1.25 MiB，接近全部位于 NUMA 1。
- 健康状态：`127.0.0.1:1236/health` 正常。

两套模型的 CPU affinity 和实际内存分布均符合预期，NUMA 隔离已经生效。它们不会直接争抢同一组 CPU 核心或本地节点内存，但仍共享整机内存互联、磁盘与网络 I/O。

## 10. 风险与优化建议

| 优先级 | 项目 | 建议 |
| --- | --- | --- |
| 高 | 10GbE 网卡只协商到 1Gbps | 核查交换机端口、线缆/模块和自动协商；条件具备时恢复 10Gbps |
| 高 | 两个物理口配置同一 IPv4、未聚合 | 第二口接通前建立正式 bond/team，或改为不同地址 |
| 中 | MiMo 使用 AVX2 后端 | 在不影响视觉投影和稳定性的前提下，用相同请求集对 AVX2/AVX-512 做基准与回归，再决定迁移 |
| 中 | SMART/BMC 数据不可读 | 由管理员补采 NVMe/HDD SMART、BMC 固件、风扇、PSU 与功耗数据 |
| 中 | 服务器已连续运行约 291 天 | 在维护窗口检查待重启更新、内核/BIOS/BMC 版本和历史硬件事件 |
| 低 | 无静态 HugeTLB | 保持现状，只有在标准化基准证明收益后再调整；不要直接在生产机试配 |

## 11. 尚未确认的项目

当前 SSH 用户没有免密 sudo，因此以下信息不能可靠给出：

- DIMM 实际数量、单条容量、厂商、料号与配置速度；
- NVMe/HDD SMART、SSD 磨损、介质错误、坏扇区和通电时长；
- BMC 型号和固件；
- 风扇、PSU 冗余状态、输入/输出功率；
- 完整历史 ECC/RAS 事件。

管理员可在维护窗口执行以下只读命令补齐：

```bash
sudo dmidecode -t 16 -t 17
sudo smartctl -x /dev/nvme0n1
sudo smartctl -x /dev/sda
sudo ipmitool mc info
sudo ipmitool sensor list
sudo ras-mc-ctl --errors
```

## 12. 主要取证命令

本报告使用的只读命令包括：

```bash
hostnamectl
cat /etc/os-release
uname -a
lscpu
lscpu --caches
numactl --hardware
free -h
lsmem
cat /proc/meminfo
lsblk
df -hT
df -ih
cat /proc/mdstat
lspci -nnk
ip -br link
ip -br address
ip -s link
ethtool enp1s0f0np0
ethtool -i enp1s0f0np0
sensors
numastat -p <PID>
taskset -pc <PID>
curl http://127.0.0.1:<PORT>/health
```
