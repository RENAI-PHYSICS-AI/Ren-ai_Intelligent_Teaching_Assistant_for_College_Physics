# DeepSeek V4 Flash AVX-512 后端部署与启动流程

> 实测环境：Rocky Linux、2 × AMD EPYC 9V94、256 CPU、565 GiB 内存、纯 CPU 推理  
> 实测日期：2026-08-10  
> 部署原则：全部编译工具和源码安装在当前用户目录，不写入系统目录，不替换现有 LM Studio 服务

## 1. 重要说明

AVX-512 不是 LM Studio 的模型启动参数，不能通过添加 `--avx512` 开启。当前服务器安装的 LM Studio Linux CPU runtime 是 AVX2 版本。要使用 AVX-512，需要针对本机 CPU 编译官方 `llama.cpp`，并作为独立 API 服务运行。

本文部署结果：

- 原有 Nginx：`0.0.0.0:1234`，保持不变；
- 原有 LM Studio/llmster：`0.0.0.0:1235`，保持不变；
- AVX-512 DeepSeek：`127.0.0.1:1236`，仅服务器本机可访问；
- 现有 Qwen 和其他 LM Studio 模型不受影响。

本机编译会使用 `-march=native`，生成的二进制只适合支持相同指令集的机器，不应直接复制到不支持 AVX-512 的服务器。

## 2. 登录服务器并检查硬件

```bash
ssh renai_server
```

检查 CPU 是否暴露 AVX-512：

```bash
lscpu | grep -i avx512

gcc -march=native -Q --help=target 2>/dev/null |
grep -E 'mavx512(f|bw|vl|dq|vnni|bf16)'
```

预期主要项目显示 `[enabled]`。本服务器已确认支持：

- AVX512F；
- AVX512BW；
- AVX512DQ；
- AVX512VL；
- AVX512VNNI；
- AVX512BF16。

检查资源：

```bash
free -h
df -h "$HOME"
lms ps
```

DeepSeek 稳态约占 146–147 GiB 内存，加载阶段应保留更大的可用内存。若 LM Studio 已经加载同一个 DeepSeek 模型，先卸载它，避免内存中同时存在两份模型：

```bash
lms unload deepseek/deepseek-v4-flash
```

不要卸载正在被其他应用使用的 Qwen 模型。

## 3. 在用户目录安装构建工具

定义目录：

```bash
TOOLS="$HOME/.local/opt/llama-build-tools"
SRC="$HOME/.local/src/llama.cpp-avx512"

mkdir -p \
  "$HOME/.local/opt" \
  "$HOME/.local/src" \
  "$HOME/.local/var/log" \
  "$HOME/.local/var/run"
```

创建独立 Python 虚拟环境并安装 CMake、Ninja：

```bash
python3 -m venv "$TOOLS"
"$TOOLS/bin/python" -m pip install --upgrade pip cmake ninja
```

整个过程不需要 `sudo`。

## 4. 下载并固定 llama.cpp 源码

本次验证通过的源码提交为：

```text
74ce15741b420b8d6f12e720398458b576c51c2c
```

首次安装：

```bash
git init "$SRC"
git -C "$SRC" remote add origin https://github.com/ggml-org/llama.cpp.git
git -C "$SRC" fetch --depth 1 origin 74ce15741b420b8d6f12e720398458b576c51c2c
git -C "$SRC" checkout --detach FETCH_HEAD

git -C "$SRC" rev-parse HEAD
```

最后一条必须输出上面的完整提交号。不要长期使用未固定的浮动 `master`，否则重建结果可能发生变化。

## 5. 配置并编译 AVX-512 后端

```bash
export PATH="$TOOLS/bin:$PATH"

cmake \
  -S "$SRC" \
  -B "$SRC/build" \
  -G Ninja \
  -DCMAKE_MAKE_PROGRAM="$TOOLS/bin/ninja" \
  -DCMAKE_BUILD_TYPE=Release \
  -DGGML_NATIVE=ON \
  -DGGML_CUDA=OFF \
  -DLLAMA_BUILD_SERVER=ON \
  -DLLAMA_BUILD_TESTS=OFF \
  -DLLAMA_BUILD_EXAMPLES=OFF \
  -DLLAMA_BUILD_UI=OFF \
  -DLLAMA_USE_PREBUILT_UI=OFF \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

cmake --build "$SRC/build" --target llama-server -j 96
```

必须同时关闭：

```text
LLAMA_BUILD_UI=OFF
LLAMA_USE_PREBUILT_UI=OFF
```

否则无图形界面需求的 API 构建仍可能等待 Hugging Face 的 Web UI 资产下载。

如果 SSH 客户端容易超时，可以让编译在后台继续：

```bash
BUILD_LOG="$HOME/.local/var/log/llama-avx512-build.log"

nohup cmake --build "$SRC/build" --target llama-server -j 96 \
  >"$BUILD_LOG" 2>&1 </dev/null &

echo $! > "$HOME/.local/var/run/llama-avx512-build.pid"
tail -f "$BUILD_LOG"
```

Ninja 支持增量构建；网络或 SSH 中断后重新执行构建命令，不会从头编译所有对象文件。

## 6. 验证 AVX-512 确实进入二进制

检查版本：

```bash
"$SRC/build/bin/llama-server" --version
```

检查编译参数：

```bash
grep -m 3 -oE -- '-march=native|-mavx512[^ " ]*' \
  "$SRC/build/compile_commands.json" |
sort -u
```

检查 CPU 动态库中的 ZMM/AVX-512 指令：

```bash
objdump -d "$SRC/build/bin/libggml-cpu.so" 2>/dev/null |
grep -m 5 -E 'zmm[0-9]+|v[a-z0-9]+.*%zmm'
```

本次实测能够看到 `vmovdqa64`、`vpbroadcastd` 等 ZMM 指令。

## 7. 启动 DeepSeek V4 Flash

设置路径：

```bash
BIN="$SRC/build/bin/llama-server"
MODEL="$HOME/.lmstudio/models/lmstudio-community/DeepSeek-V4-Flash-0731-GGUF/DeepSeek-V4-Flash-0731-MXFP4-00001-of-00004.gguf"
LOG="$HOME/.local/var/log/llama-avx512-deepseek.log"
PIDFILE="$HOME/.local/var/run/llama-avx512-deepseek.pid"
```

确认模型和端口：

```bash
test -x "$BIN"
test -r "$MODEL"
ss -lntp | grep ':1236 ' || true
```

后台启动：

```bash
: > "$LOG"

nohup "$BIN" \
  --model "$MODEL" \
  --alias deepseek/deepseek-v4-flash-avx512 \
  --host 127.0.0.1 \
  --port 1236 \
  --ctx-size 8192 \
  --parallel 4 \
  --kv-unified \
  --threads 96 \
  --threads-batch 96 \
  --batch-size 2048 \
  --ubatch-size 512 \
  --jinja \
  >"$LOG" 2>&1 </dev/null &

echo $! > "$PIDFILE"
```

`--kv-unified` 不可遗漏。若在 4 路并发下直接使用 `--ctx-size 8192` 而不启用统一 KV，日志可能显示每个槽只有 2048 token。正确配置应显示：

```text
n_slots = 4, n_ctx_slot = 8192, kv_unified = 'true'
```

统一 KV 表示四个槽共享缓冲区；每个槽允许使用完整的 8192 token 上限，但多个长上下文并发时仍会共同竞争容量和带宽。

## 8. 等待模型就绪

DeepSeek 的 4 个分片合计约 145.6 GiB。本机实测冷加载约 92.6 秒。在加载完成前，health 返回 503 属于正常现象。

查看加载日志：

```bash
tail -f "$LOG"
```

或者等待健康检查通过：

```bash
for i in $(seq 1 180); do
  if curl -fsS --max-time 2 http://127.0.0.1:1236/health; then
    echo
    echo "AVX-512 服务已就绪"
    break
  fi

  if ! kill -0 "$(cat "$PIDFILE")" 2>/dev/null; then
    echo "服务进程已经退出，请检查：$LOG"
    exit 1
  fi

  sleep 1
done
```

## 9. 健康检查与模型信息

```bash
curl -fsS http://127.0.0.1:1236/health
curl -fsS http://127.0.0.1:1236/v1/models
curl -fsS http://127.0.0.1:1236/slots | python3 -m json.tool
```

预期 health：

```json
{"status":"ok"}
```

确认运行进程实际加载的是本次编译的 CPU 库：

```bash
PID="$(cat "$PIDFILE")"
grep 'libggml-cpu' "/proc/$PID/maps" | head
```

路径应指向：

```text
$HOME/.local/src/llama.cpp-avx512/build/bin/libggml-cpu.so.0.19.0
```

## 10. 发送测试请求

普通助教回答建议关闭长思考，否则 DeepSeek 可能把全部输出额度用于 `reasoning_content`，导致正文为空。

```bash
curl -fsS http://127.0.0.1:1236/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "deepseek/deepseek-v4-flash-avx512",
    "messages": [
      {
        "role": "user",
        "content": "一个质量为2 kg的物体速度从3 m/s增加到5 m/s，动能增加多少焦耳？请给出公式和结果。"
      }
    ],
    "reasoning_effort": "none",
    "temperature": 0,
    "max_tokens": 256,
    "stream": false
  }'
```

本次实测正确结果为 16 J。

复杂问题需要保留思考时，可以去掉 `reasoning_effort`，但必须为内部推理预留足够的输出 token。

## 11. 查看、停止和重新启动

查看状态：

```bash
PID="$(cat "$PIDFILE")"
ps -o pid,ppid,stat,etime,%cpu,%mem,rss,cmd -p "$PID"
curl -fsS http://127.0.0.1:1236/health
```

查看日志：

```bash
tail -f "$LOG"
```

停止服务：

```bash
PID="$(cat "$PIDFILE")"
kill -TERM "$PID"

for i in $(seq 1 20); do
  kill -0 "$PID" 2>/dev/null || break
  sleep 1
done

if kill -0 "$PID" 2>/dev/null; then
  echo "服务未在 20 秒内退出，请先检查日志，不要直接重复启动。"
fi
```

重新启动时，重复第 7、8 节即可。

当前使用 `nohup` 常驻，服务器重启后不会自动恢复。若要配置开机自启，建议另建用户级 systemd 服务；在确认对外路由和模型使用策略前，不要替换现有 LM Studio 服务。

## 12. 与现有项目或 Nginx 集成

当前端口仅监听 `127.0.0.1`，适合由同机应用访问：

```text
http://127.0.0.1:1236/v1
```

不要直接把现有 Nginx `/v1/` 全部切换到 1236，否则 Qwen 和其他 LM Studio 模型会失去原来的入口。更安全的方案是：

1. 为 DeepSeek AVX-512 增加独立反向代理路径；或
2. 在应用内部按模型选择不同上游；或
3. 确认只需要 DeepSeek 后，再切换整个 `/v1/` 上游。

如果确实需要局域网访问，应通过带访问控制的 Nginx 反向代理暴露；不要把无 API key 的 `llama-server` 直接绑定到 `0.0.0.0`。

## 13. 常见问题

### 13.1 CMake 找不到 Ninja

错误示例：

```text
CMake was unable to find a build program corresponding to "Ninja"
```

解决：

```bash
export PATH="$TOOLS/bin:$PATH"

cmake \
  -S "$SRC" \
  -B "$SRC/build" \
  -G Ninja \
  -DCMAKE_MAKE_PROGRAM="$TOOLS/bin/ninja" \
  ...
```

### 13.2 编译卡在 UI 资产下载

重新配置时同时加入：

```bash
-DLLAMA_BUILD_UI=OFF \
-DLLAMA_USE_PREBUILT_UI=OFF
```

随后重新运行增量编译。

### 13.3 health 返回 503

模型仍在加载时属于正常情况。检查：

```bash
tail -n 100 "$LOG"
free -h
```

看到下面内容后才算就绪：

```text
model loaded
listening on http://127.0.0.1:1236
```

### 13.4 回答正文为空

检查响应是否只有 `reasoning_content`。普通问答加入：

```json
"reasoning_effort": "none"
```

或者提高 `max_tokens` 并显式设置思考预算。

### 13.5 每槽上下文变成 2048

确认启动参数包含：

```bash
--ctx-size 8192 --parallel 4 --kv-unified
```

### 13.6 误报没有 GPU

本方案是纯 CPU 编译，不需要 GPU。不要添加 `--n-gpu-layers`；纯 CPU 服务会自动使用 CPU 后端。

### 13.7 服务重复启动或端口占用

```bash
ss -lntp | grep ':1236 '
cat "$PIDFILE"
ps -fp "$(cat "$PIDFILE")"
```

先确认旧进程身份，再按第 11 节正常停止；不要直接启动第二份 146 GiB 模型。

## 14. 本次实测性能摘要

| 并发 | LM Studio AVX2 聚合吞吐 | 原生 AVX-512 聚合吞吐 |
|---:|---:|---:|
| 1 | 约 6.3 token/s | 6.73 token/s |
| 2 | 8.6–9.3 token/s | 11.46 token/s |
| 4 | 15.4–15.8 token/s | 19.50–20.69 token/s |

4 路两次复测平均约 20.09 token/s，相对 AVX2 中心值约提升 28.8%。详细数据见同目录的《模型承载能力测试报告.md》。

## 15. 当前部署路径

```text
构建工具：$HOME/.local/opt/llama-build-tools
源码目录：$HOME/.local/src/llama.cpp-avx512
服务程序：$HOME/.local/src/llama.cpp-avx512/build/bin/llama-server
运行日志：$HOME/.local/var/log/llama-avx512-deepseek.log
PID 文件：$HOME/.local/var/run/llama-avx512-deepseek.pid
API 地址：http://127.0.0.1:1236/v1
```

本文不包含 API 密钥，也不要求修改系统目录。
