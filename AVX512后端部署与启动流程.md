# DeepSeek V4 Flash AVX-512 后端部署与启动流程

> 实测环境：Rocky Linux、2 × AMD EPYC 9V94、256 CPU、565 GiB 内存、纯 CPU 推理  
> 实测日期：2026-08-10  
> 部署原则：全部编译工具和源码安装在当前用户目录，不写入系统目录，不替换现有 LM Studio 服务

## 1. 重要说明

AVX-512 不是 LM Studio 的模型启动参数，不能通过添加 `--avx512` 开启。当前服务器安装的 LM Studio Linux CPU runtime 是 AVX2 版本。要使用 AVX-512，需要针对本机 CPU 编译官方 `llama.cpp`，并作为独立 API 服务运行。

本文部署结果：

- 原有 Nginx：`0.0.0.0:1234`，保持不变；
- MiMo-VL AVX2：`127.0.0.1:1237`，NUMA node 0 / CPU `0-127`，128K、4 槽；
- AVX-512 DeepSeek：`127.0.0.1:1236`，NUMA node 1 / CPU `128-255`，1048576、单槽；
- 两个接口仅服务器本机可访问，均由用户级 systemd 常驻。

本机编译会使用 `-march=native`，生成的二进制只适合支持相同指令集的机器，不应直接复制到不支持 AVX-512 的服务器。

## 2. 登录服务器并检查硬件

```bash
ssh renai_server
```

检查 CPU 是否暴露 AVX-512：

```bash
lscpu | grep -i avx512

gcc -march=native -Q --help=target 2>/dev/null |
grep -E 'mavx512(f|bw|vl|dq|vnni|bf16)'
```

预期主要项目显示 `[enabled]`。本服务器已确认支持：

- AVX512F；
- AVX512BW；
- AVX512DQ；
- AVX512VL；
- AVX512VNNI；
- AVX512BF16。

检查资源：

```bash
free -h
df -h "$HOME"
lms ps
```

DeepSeek 稳态约占 146–147 GiB 内存，加载阶段应保留更大的可用内存。若 LM Studio 已经加载同一个 DeepSeek 模型，先卸载它，避免内存中同时存在两份模型：

```bash
lms unload deepseek/deepseek-v4-flash
```

不要卸载正在被其他应用使用的 Qwen 模型。

## 3. 在用户目录安装构建工具

定义目录：

```bash
TOOLS="$HOME/.local/opt/llama-build-tools"
SRC="$HOME/.local/src/llama.cpp-avx512"

mkdir -p \
  "$HOME/.local/opt" \
  "$HOME/.local/src" \
  "$HOME/.local/var/log" \
  "$HOME/.local/var/run"
```

创建独立 Python 虚拟环境并安装 CMake、Ninja：

```bash
python3 -m venv "$TOOLS"
"$TOOLS/bin/python" -m pip install --upgrade pip cmake ninja
```

整个过程不需要 `sudo`。

## 4. 下载并固定 llama.cpp 源码

本次验证通过的源码提交为：

```text
74ce15741b420b8d6f12e720398458b576c51c2c
```

首次安装：

```bash
git init "$SRC"
git -C "$SRC" remote add origin https://github.com/ggml-org/llama.cpp.git
git -C "$SRC" fetch --depth 1 origin 74ce15741b420b8d6f12e720398458b576c51c2c
git -C "$SRC" checkout --detach FETCH_HEAD

git -C "$SRC" rev-parse HEAD
```

最后一条必须输出上面的完整提交号。不要长期使用未固定的浮动 `master`，否则重建结果可能发生变化。

## 5. 配置并编译 AVX-512 后端

```bash
export PATH="$TOOLS/bin:$PATH"

cmake \
  -S "$SRC" \
  -B "$SRC/build" \
  -G Ninja \
  -DCMAKE_MAKE_PROGRAM="$TOOLS/bin/ninja" \
  -DCMAKE_BUILD_TYPE=Release \
  -DGGML_NATIVE=ON \
  -DGGML_CUDA=OFF \
  -DLLAMA_BUILD_SERVER=ON \
  -DLLAMA_BUILD_TESTS=OFF \
  -DLLAMA_BUILD_EXAMPLES=OFF \
  -DLLAMA_BUILD_UI=OFF \
  -DLLAMA_USE_PREBUILT_UI=OFF \
  -DCMAKE_EXPORT_COMPILE_COMMANDS=ON

cmake --build "$SRC/build" --target llama-server -j 96
```

必须同时关闭：

```text
LLAMA_BUILD_UI=OFF
LLAMA_USE_PREBUILT_UI=OFF
```

否则无图形界面需求的 API 构建仍可能等待 Hugging Face 的 Web UI 资产下载。

如果 SSH 客户端容易超时，可以让编译在后台继续：

```bash
BUILD_LOG="$HOME/.local/var/log/llama-avx512-build.log"

nohup cmake --build "$SRC/build" --target llama-server -j 96 \
  >"$BUILD_LOG" 2>&1 </dev/null &

echo $! > "$HOME/.local/var/run/llama-avx512-build.pid"
tail -f "$BUILD_LOG"
```

Ninja 支持增量构建；网络或 SSH 中断后重新执行构建命令，不会从头编译所有对象文件。

## 6. 验证 AVX-512 确实进入二进制

检查版本：

```bash
"$SRC/build/bin/llama-server" --version
```

检查编译参数：

```bash
grep -m 3 -oE -- '-march=native|-mavx512[^ " ]*' \
  "$SRC/build/compile_commands.json" |
sort -u
```

检查 CPU 动态库中的 ZMM/AVX-512 指令：

```bash
objdump -d "$SRC/build/bin/libggml-cpu.so" 2>/dev/null |
grep -m 5 -E 'zmm[0-9]+|v[a-z0-9]+.*%zmm'
```

本次实测能够看到 `vmovdqa64`、`vpbroadcastd` 等 ZMM 指令。

## 7. 安装用户级 systemd 服务

仓库的两个应用镜像都包含以下三个同内容文件：

```text
agnet/deepseek-avx512.service
agnet/deepseek-avx512.env.example
agnet/install_deepseek_avx512_service.sh
```

安装器只写当前用户的 `~/.config/systemd/user/` 和 `~/.config/physics-assistant/`，不写系统级 unit，也不会部署到 `/etc`。首次执行会安装 unit、创建权限为 `0600` 的专用环境文件，然后停止，不会以空模型路径或空密钥启动：

```bash
cd "$HOME/agent_of_college_physics"
bash agnet/install_deepseek_avx512_service.sh
```

编辑本机配置：

```bash
vi "$HOME/.config/physics-assistant/deepseek-avx512.env"
```

必须填写：

- `DEEPSEEK_AVX512_SERVER_BIN`：本机编译出的 `llama-server` 绝对路径；
- `DEEPSEEK_AVX512_MODEL_PATH`：DeepSeek GGUF 第一个分片的绝对路径；
- `LLAMA_API_KEY`：只供该 API 使用的随机密钥，可用 `openssl rand -hex 32` 生成；这是固定 llama.cpp 版本官方支持的环境变量，不会出现在进程 argv。

不要把真实模型路径或密钥写进 unit、示例文件或 Git。其余生产约束已在环境示例中明确：

```ini
DEEPSEEK_AVX512_HOST=127.0.0.1
DEEPSEEK_AVX512_PORT=1236
DEEPSEEK_AVX512_CTX_SIZE=1048576
DEEPSEEK_AVX512_PARALLEL=1
DEEPSEEK_AVX512_NUMA_NODE=1
DEEPSEEK_AVX512_CPU_LIST=128-255
DEEPSEEK_AVX512_THREADS=128
DEEPSEEK_AVX512_THREADS_BATCH=128
```

安装器会拒绝改变回环监听、`1048576` 上下文、单槽、node 1 或 CPU `128-255` 的配置，并在启动前检查二进制、模型首分片、NUMA 节点和 CPU 是否存在。填写完成后再次执行：

```bash
bash agnet/install_deepseek_avx512_service.sh
```

脚本随后启用 unit；首次启动，已运行时则重启以加载新配置。unit 实际使用：

```text
numactl --physcpubind=128-255 --membind=1 llama-server --load-mode dio
```

因此推理线程只在 CPU `128-255` 运行，模型内存严格从 NUMA node 1 分配；node 1 内存耗尽时分配失败，不会回退到其他节点。Direct I/O 是必需项：若使用 mmap，Linux 可能复用 node 0 的文件页缓存，即使外层设置 `--membind=1`，权重页仍会出现在 `numa_maps` 的 `file ... N0=` 中；Direct I/O 绕过该共享页缓存，并把读取结果分配到受 node 1 策略约束的内存。不要再向 llama-server 传入内部 `--numa numactl`：该后端会重新扩大 CPU 亲和性，抵消外层 CPU 绑定；本服务只使用外层 numactl。也不要额外添加 `numactl --strict`：该选项只用于共享内存策略检查，在普通进程启动模式会被忽略。安装器会逐一确认 CPU `128-255` 在线且都属于 node 1，并在启用服务前执行同参数的 `numactl` dry-run。启动前还应确认 node 1 有足够可用内存；DeepSeek 的 4 个分片合计约 145.6 GiB，加载期间需要额外余量。

unit 配置为 `Restart=always` 和 15 秒重启间隔。它负责进程异常退出后的恢复；若还要求服务器重启后在无人登录时自动启动，需由管理员为运行用户启用 linger：

```bash
sudo loginctl enable-linger 运行用户名
```

这一步属于服务器管理员操作，项目安装器不会调用 `sudo`。

## 8. 等待模型就绪

本机较小上下文此前实测冷加载约 92.6 秒；当前 1M KV 缓冲区会增加初始化时间。Direct I/O 会读取全部权重并在 node 1 分配，加载完成前 health 返回 503 属于正常现象，应用默认最多等待 1800 秒。

查看加载日志：

```bash
journalctl --user -u deepseek-avx512.service -f
```

或者等待健康检查通过：

```bash
for i in $(seq 1 1800); do
  if curl -fsS --max-time 2 http://127.0.0.1:1236/health; then
    echo
    echo "AVX-512 服务已就绪"
    break
  fi

  if ! systemctl --user is-active --quiet deepseek-avx512.service; then
    echo "服务未运行，请检查 journal。" >&2
    exit 1
  fi

  sleep 1
done
```

正常启动日志应显示一个槽和完整上下文：

```text
n_slots = 1, n_ctx_slot = 1048576
```

`--parallel 1` 的目标是让教研考试等长请求独占完整上下文，避免多个并发槽切分 KV 容量和 NUMA 带宽。该服务不是此前 4 路吞吐测试配置。

加载完成后必须核验实际 NUMA 落点，而不只看 unit 参数：

```bash
PID="$(systemctl --user show -p MainPID --value deepseek-avx512.service)"
numastat -p "$PID"
grep -E 'anon|heap|file' "/proc/$PID/numa_maps" | head -n 30
```

DeepSeek 的私有权重内存应主要位于 `N1`；若权重仍以大块 `file ... N0=` 出现，说明运行单元没有真正使用 Direct I/O，应停止排查，不能把该状态视为 NUMA1 部署成功。

## 9. 健康检查与模型信息

先读取专用环境文件，命令不会在文档或仓库中出现真实密钥：

```bash
set -a
source "$HOME/.config/physics-assistant/deepseek-avx512.env"
set +a

curl -fsS http://127.0.0.1:1236/health
curl -fsS \
  -H "Authorization: Bearer $LLAMA_API_KEY" \
  http://127.0.0.1:1236/v1/models
curl -fsS \
  -H "Authorization: Bearer $LLAMA_API_KEY" \
  http://127.0.0.1:1236/slots | python3 -m json.tool
```

预期 health：

```json
{"status":"ok"}
```

确认主进程确实处于要求的 CPU 和内存策略，并加载本次编译的 CPU 库：

```bash
PID="$(systemctl --user show deepseek-avx512.service -p MainPID --value)"
taskset -pc "$PID"
grep -E 'Cpus_allowed_list|Mems_allowed_list' "/proc/$PID/status"
grep 'libggml-cpu' "/proc/$PID/maps" | head
```

预期 `Cpus_allowed_list` 为 `128-255`，`Mems_allowed_list` 为 `1`；CPU 动态库路径应位于当前配置的 AVX-512 `llama-server` 构建目录。

## 10. 发送测试请求

普通助教回答建议关闭长思考，否则 DeepSeek 可能把全部输出额度用于 `reasoning_content`，导致正文为空。

```bash
curl -fsS http://127.0.0.1:1236/v1/chat/completions \
  -H "Authorization: Bearer $LLAMA_API_KEY" \
  -H 'Content-Type: application/json' \
  -d '{
    "model": "deepseek/deepseek-v4-flash-avx512",
    "messages": [
      {
        "role": "user",
        "content": "一个质量为2 kg的物体速度从3 m/s增加到5 m/s，动能增加多少焦耳？请给出公式和结果。"
      }
    ],
    "reasoning_effort": "none",
    "temperature": 0,
    "max_tokens": 256,
    "stream": false
  }'
```

本次实测正确结果为 16 J。

复杂问题需要保留思考时，可以去掉 `reasoning_effort`，但必须为内部推理预留足够的输出 token。

## 11. 查看、停止和重新启动

```bash
systemctl --user status deepseek-avx512.service
journalctl --user -u deepseek-avx512.service -n 100 --no-pager
journalctl --user -u deepseek-avx512.service -f
```

停启与重启：

```bash
systemctl --user stop deepseek-avx512.service
systemctl --user start deepseek-avx512.service
systemctl --user restart deepseek-avx512.service
```

修改 `~/.config/physics-assistant/deepseek-avx512.env` 后必须重启服务。不要另行用 `nohup` 启动第二份 146 GiB 模型；systemd 是该独立后端唯一的进程管理入口。

## 12. 与现有项目或 Nginx 集成

当前端口仅监听 `127.0.0.1`，适合由同机应用访问：

```text
http://127.0.0.1:1236/v1
```

Rocky 应用只把教研考试路由到该服务；普通问答和识图继续使用主模型：

```ini
PHYSICS_EXAM_BASE_URL=http://127.0.0.1:1236/v1
PHYSICS_EXAM_MODEL=deepseek/deepseek-v4-flash-avx512
PHYSICS_EXAM_API_KEY=
PHYSICS_EXAM_NO_THINK_SUFFIX=
PHYSICS_EXAM_TIMEOUT_SECONDS=900
PHYSICS_EXAM_CONTEXT_WINDOW=1048576
PHYSICS_EXAM_MAX_OUTPUT_TOKENS=32768
```

`PHYSICS_EXAM_API_KEY` 留空时应用回退 `PHYSICS_API_KEY`；最终有效密钥必须与 `~/.config/physics-assistant/deepseek-avx512.env` 中的 `LLAMA_API_KEY` 相同。DeepSeek 不接收 MiMo 专用 `/no_think`，所以考试后缀保持空值。修改应用配置后执行 `bash manage.sh restart`。

不要直接把现有 Nginx `/v1/` 全部切换到 1236，否则 Qwen 和其他 LM Studio 模型会失去原来的入口。更安全的方案是：

1. 为 DeepSeek AVX-512 增加独立反向代理路径；或
2. 在应用内部按模型选择不同上游；或
3. 确认只需要 DeepSeek 后，再切换整个 `/v1/` 上游。

如果确实需要局域网访问，应通过带访问控制的 Nginx 反向代理暴露并保留 Bearer 鉴权；不要把 `llama-server` 直接绑定到 `0.0.0.0`。本次只新增本地服务文件，没有部署或修改任何服务器、Nginx、端口、防火墙与应用路由。

## 13. 常见问题

### 13.1 CMake 找不到 Ninja

错误示例：

```text
CMake was unable to find a build program corresponding to "Ninja"
```

解决：

```bash
export PATH="$TOOLS/bin:$PATH"

cmake \
  -S "$SRC" \
  -B "$SRC/build" \
  -G Ninja \
  -DCMAKE_MAKE_PROGRAM="$TOOLS/bin/ninja" \
  ...
```

### 13.2 编译卡在 UI 资产下载

重新配置时同时加入：

```bash
-DLLAMA_BUILD_UI=OFF \
-DLLAMA_USE_PREBUILT_UI=OFF
```

随后重新运行增量编译。

### 13.3 health 返回 503

模型仍在加载时属于正常情况。检查：

```bash
tail -n 100 "$LOG"
free -h
```

看到下面内容后才算就绪：

```text
model loaded
listening on http://127.0.0.1:1236
```

### 13.4 回答正文为空

检查响应是否只有 `reasoning_content`。普通问答加入：

```json
"reasoning_effort": "none"
```

或者提高 `max_tokens` 并显式设置思考预算。

### 13.5 未得到一个 1048576-token 槽

检查环境文件：

```bash
grep -E 'DEEPSEEK_AVX512_(CTX_SIZE|PARALLEL)=' \
  "$HOME/.config/physics-assistant/deepseek-avx512.env"
```

必须分别为 `1048576` 和 `1`。修改后运行 `systemctl --user restart deepseek-avx512.service`，并在 journal 中确认 `n_slots = 1, n_ctx_slot = 1048576`。

### 13.6 误报没有 GPU

本方案是纯 CPU 编译，不需要 GPU。不要添加 `--n-gpu-layers`；纯 CPU 服务会自动使用 CPU 后端。

### 13.7 服务重复启动或端口占用

```bash
ss -lntp | grep ':1236 '
systemctl --user status deepseek-avx512.service
systemctl --user show deepseek-avx512.service -p MainPID --value
```

先确认旧进程身份，再按第 11 节正常停止；不要用 `nohup` 直接启动第二份 146 GiB 模型。

### 13.8 服务在退出 SSH 后停止

检查当前用户是否启用 linger：

```bash
loginctl show-user "$(id -un)" -p Linger
```

若输出 `Linger=no`，请让管理员执行第 7 节的 `loginctl enable-linger`。这只影响用户级 systemd manager 是否在无人登录时保持，不需要也不应把 unit 复制到 `/etc/systemd/system`。

## 14. 本次实测性能摘要

| 并发 | LM Studio AVX2 聚合吞吐 | 原生 AVX-512 聚合吞吐 |
|---:|---:|---:|
| 1 | 约 6.3 token/s | 6.73 token/s |
| 2 | 8.6–9.3 token/s | 11.46 token/s |
| 4 | 15.4–15.8 token/s | 19.50–20.69 token/s |

4 路两次复测平均约 20.09 token/s，相对 AVX2 中心值约提升 28.8%。这些是此前 `ctx=8192`、`parallel=4` 的吞吐基准，仅用于证明 AVX-512 后端有效；本次常驻 unit 使用 `ctx=1048576`、`parallel=1`，面向长上下文完整性，不能直接套用 4 路吞吐数字。详细数据见同目录的《模型承载能力测试报告.md》。

## 15. 当前部署路径

```text
构建工具：$HOME/.local/opt/llama-build-tools
源码目录：$HOME/.local/src/llama.cpp-avx512
服务程序：由 DEEPSEEK_AVX512_SERVER_BIN 指定
模型首分片：由 DEEPSEEK_AVX512_MODEL_PATH 指定
专用配置：$HOME/.config/physics-assistant/deepseek-avx512.env
用户 unit：$HOME/.config/systemd/user/deepseek-avx512.service
运行日志：journalctl --user -u deepseek-avx512.service
API 地址：http://127.0.0.1:1236/v1
```

本文与仓库示例均不包含实际模型路径或 API 密钥，也不要求修改系统目录。此次改动只保存在本地工作区，未部署到 Rocky 服务器。
